# 변경 이력

IntentTrace의 사용자와 운영자에게 영향을 주는 변경을 기록합니다.

## 미출시

### 추가

- GitHub 승인 취소 웹훅으로 해당 사용자의 브라우저·도구 세션 폐기
- 홈서버 k3s의 앱 1개·PostgreSQL PVC·Traefik Ingress, 임시 환경 예시와 배포 파일 검증
- IntelliJ 설정에서 저장 세션의 유효 여부와 GitHub 계정을 확인하는 로그인 확인
- 파일 수정 후 지역 검사와 종료 전 전체 diff·ArchUnit 구조 검사를 실행하는 Codex 훅과 공통 명령
- 웹 조회의 GitHub 일시 장애·호출 제한 화면에서 같은 조건과 페이지로 다시 조회
- IntelliJ 기록 상세에서 같은 서버의 웹 기록으로 이동해 변경 이력·원본 비교·Markdown 저장 사용
- Zed 연결 점검 명령의 PR 번호·커밋 지정과 PR 현재 커밋 읽기 확인
- IntelliJ 기록함의 현재 페이지 새로고침과 기록 ID에 따른 선택 유지
- 웹 팀 기록 목록에서 내 공개 기록만 조회하고 작성자 필터를 해제하는 바로가기
- IntelliJ 후속 기록 상세에서 원본 기록 표시와 바로 이동
- 웹 CI 결과의 이전 페이지·새로고침·페이지 번호 표시와 PR 내용·변경 기록 화면 간 이동
- IntelliJ 현재 줄 결과에서 선택 기록 상세와 조회한 파일의 과거 기록으로 바로 이동
- 검색 목록에서 기록을 연 뒤 같은 검색어·필터·페이지로 복귀하는 링크와 파일·줄 조회 결과의 당시·현재 커밋 코드 바로가기
- 웹 기록 상세의 Markdown 파일 저장과 PR 기록 화면에서 표시 중인 커밋의 CI 결과로 이동
- 초안에 사용할 이슈·PR 내용과 기존 GitHub Actions 결과를 조회하는 REST·MCP·브라우저 화면
- 외부 요청의 출처·발췌 표시와 CI 재실행 차수·커밋·상태 구분. 새 CI 실행이나 유료 서비스 추가 없이 읽기 API만 사용
- 현재 `its_` 세션을 폐기하는 `DELETE /api/v1/session`과 IntelliJ PasswordSafe 연결 해제
- 사용자별 활성 세션 기본 5개 상한과 초과 시 가장 오래된 세션 자동 폐기
- 저장소·파일·상태별 팀 공개 기록과 내 비공개 기록 목록 REST·MCP (`list_change_records`)
- 기존 공개 기록을 후속 공개 기록으로 연결하는 MCP `supersede_change_record`와 Codex 대체 절차
- IntelliJ 기록함, 현재 줄 결과가 없을 때 파일 이력 탐색, 원래 커밋·코드 근거·대체 기록 이동
- IntelliJ 서버 주소 설정과 인증 정보 없는 서버 상태 확인, IDE 재시작 없는 주소 적용

### 변경

- 잘못된 GitHub PR 응답을 사용자 입력 오류로 안내하던 문제 수정. 연결 진단은 PR 확인 실패를 표시하고 나머지 점검 계속
- 기존 세션의 GitHub 응답 지연이 다른 사용자의 로그인과 같은 사용자의 새 로그인을 막던 오류 수정
- 브라우저 로그인 실패 후 재시도하면 도구용 토큰 발급 화면으로 바뀌던 오류 수정. 원래 기록 화면과 검색 조건 유지
- GitHub 저장소 권한 조회의 성공·실패가 앱 설치 호출로 집계되던 지표 오류 수정
- k3s의 앱·DB 설정을 분리해 GitHub 설정 변경 시 불필요한 PostgreSQL 재시작 방지
- DB 장애를 readiness에 반영하고 liveness와 분리해 불필요한 앱 재시작 방지
- IntelliJ 저장 세션 삭제 시 세션이 없거나 만료돼도 서버에서 폐기했다고 안내하던 오류 수정
- IntelliJ 세션 연결 시 서버에서 계정을 확인한 뒤 저장하고, 확인 실패 시 기존 세션 유지
- Zed의 알 수 없는 명령이 성공으로 종료되던 오류 수정과 연결 없이 확인하는 명령별 도움말
- 웹에서 변경 이력·원본 비교·코드 확인과 원본·대체 기록을 거쳐도 검색 조건과 페이지 유지
- 웹 조회 범위 탭 중복과 이전 `MY_DRAFTS` 주소의 비공개 상태 필터 표시 오류 수정
- IntelliJ 조회·연결 확인·세션 해제의 호출 제한 응답에 재시도 대기 시간 표시
- IntelliJ에서 다른 커밋의 검증 결과를 스냅샷 불일치로 표시하던 오류 수정과 검증 수집 출처 표시
- IntelliJ가 변경 전 코드도 변경 후 커밋으로 열던 오류 수정. 코드의 변경 전·후 표시와 선택한 커밋에 따른 이동 버튼 활성화
- 기록 비교·Markdown의 이름 변경 경로를 변경 전·후로 구분하고 파일 내용 일치·조회 중단·검증 결과 안내와 릴리스 문구 정리
- 로그인 오류를 사용자 재시도·운영자 설정 문제로 구분하고 게시 점검 결과와 Zed 재조회 안내를 구체화
- IntelliJ의 기록 출처를 브라우저와 같은 표현으로 맞추고 로그인·오류 안내와 문서의 인증·기록 설명을 구체화
- HTML과 Markdown 다운로드 응답의 캐시 방지·보안 헤더 구성을 공유
- 기록 비교 화면의 문자열 변환 결과를 상세·전체 보기에서 재사용하고 연결 진단의 불필요한 예외 재전달 제거
- MCP의 커서·페이지 번호 조회 설명을 구분하고 버전 값의 출처, 폐기 기록 조회 조건, 과거 코드의 줄 번호 기준 명시
- GitHub App 권한을 기능별 표로 모으고 설치 명령 중복 제거. 목록 조회 방식과 Zed 팀 서버 주소의 미리보기·적용 예시 구분
- 이전 기록 조회의 완료·부분 결과·재개 설명을 현재 동작에 맞추고 로컬 빌드 명령과 IntelliJ·운영 문서 용어 통일
- 권한·마이그레이션 안내를 현재 구현에 맞추고 README는 사용 안내, ADR은 내부 규칙 중심으로 정리
- 화면·IntelliJ·오류·사용 안내의 문구를 간결하게 통일하고 CI의 실행 대기·실행 중·결과 미확인을 구분
- 내용 해시는 `DigestOutputStream`, 줄 해시는 `MessageDigest.update`로 계산해 임시 바이트 배열 복사 제거
- 이름 변경 판별에서 중복 blob을 발견하면 남은 순회 중단
- Git 트리와 기록 비교의 중복 검사에 기존 Map을 활용해 임시 목록 생성 제거
- 이력 재개·실패 재조회에서 같은 기록의 반복 읽기를 요청당 한 번으로 줄임
- 전체 세션 종료 시 세션마다 전체 Map을 다시 검색하던 처리 제거
- PR 기록 목록의 게시 결과·최신 시도를 일괄 조회해 기록마다 반복하던 SQL 제거
- 검색 SQL을 이름 기반 매개변수로 바꿔 상태 목록과 반복 검색어·커서 시각의 수동 바인딩 제거
- GitHub API 클라이언트 다섯 곳의 주소·기본 헤더 설정을 공통 `RestClient` Bean으로 통합
- App JWT 조립·서명을 `NimbusJwtEncoder`로 전환하고 기존 PKCS1·PKCS8 개인 키 형식 유지
- 인증 오류 JSON을 Jackson으로 직렬화하고 저장소 빈 값 중복 검사와 Duration 양수 표현 정리
- 부분·전체·PostgreSQL 테스트 작업을 분리해 검증 결과 덮어쓰기 방지
- PostgreSQL 검증용 로컬 포트를 자동 배정해 다른 작업과 고정 포트 충돌 방지
- Zed 실행 파일·의존성 명세와 Python 검증 스크립트를 Gradle 테스트 입력에 포함
- 저장된 테스트 결과 집계 명령과 작업 재개 시 검증 재사용 기준 추가
- Codex 지시를 공통 규칙·개발·기록 사용으로 나누고 이미 받은 승인 재확인과 전체 문서 필수 읽기 제거
- 기록 조회는 커서 방식을 기본으로 안내하고 이전 페이지 번호 방식과 인자를 섞지 않도록 정리
- GitHub 저장소 권한 확인을 전체 저장소 목록 순회에서 대상 저장소의 사용자별 단건 권한 조회로 변경
- 증거 생성·PostgreSQL backup·smoke 검증이 종료 신호 뒤에도 계속 실행될 수 있던 문제 수정
- 코드 근거의 줄 상한을 도메인 규칙으로 통일하고 Git `blob` 검사와 내용 읽기를 한 번의 명령으로 통합
- Git 트리·파일 읽기 실패가 빈 내용의 해시로 성공 처리되던 문제 수정
- 같은 경로의 PostgreSQL backup이 동시에 실행될 때 성공한 파일을 덮어쓰거나 다른 실행의 실패 정리에서 삭제하던 문제 수정
- 코드 근거 생성 시 셸 정수 범위를 넘는 줄 번호가 빈 내용의 해시로 성공 처리되던 문제를 수정하고 서버와 같은 줄 번호 상한 적용
- Git 스냅샷의 파일명 표시 설정을 고정해 한글 파일명이 있어도 개인 Git 설정에 따라 해시가 달라지지 않도록 수정
- 코드 근거 생성 시 Git `blob` 타입을 확인해 디렉터리 목록이 파일 내용으로 해싱되던 문제 수정
- 서버가 허용한 큰 기록을 IntelliJ에서도 조회할 수 있도록 응답 상한을 4MiB로 조정하고 상한 초과 거부 유지
- Windows 서버에서 정규화한 저장소 경로에 역슬래시가 들어가 기록 생성이 실패하던 문제 수정
- IntelliJ 기록함 조회 실패 시 마지막 성공 필터를 복원하고 기존 목록·선택·페이지 유지
- IntelliJ 검증 결과를 현재 IDE 코드가 아닌 기록 스냅샷과의 일치 여부로 표시
- IntelliJ 조회 응답을 Kotlin serialization으로 읽고, 잘못된 필드 자료형을 응답 형식 오류로 처리
- IntelliJ에서 IPv6 loopback HTTP 주소를 거부하던 검사 수정
- IntelliJ 조회·세션 작업의 완료·실패 처리를 Task 콜백으로 통일하고 취소 예외를 플랫폼에 전달
- IntelliJ HTTP 요청을 SDK API로 전환해 응답 본문이 멈춰도 읽기 제한 시간이 적용되도록 수정
- OAuth callback에서 긴 IPv6 loopback 주소 표기 허용
- 나노초 검증 시각을 DB와 같은 마이크로초 정밀도로 반올림해 동일 요청 재시도 충돌 수정
- 대체 대상 두 기록을 같은 트랜잭션에서 잠가 동시 요청의 순환 참조 방지
- 비밀값 제거 후 저장 길이를 초과하면 DB 오류 대신 원문 없는 입력 오류 반환
- 같은 기록을 서로 다른 PR에 동시에 게시해도 Check Run이 중복 생성되지 않도록 잠금을 기록 단위로 변경
- OAuth 갱신 응답 수신·파싱·token 값 변환에 실패하면 세션을 폐기하고 `401`로 재승인을 요청하도록 변경
- 잠금을 기다리던 요청이 앞선 요청에서 폐기한 세션과 refresh token을 다시 사용하지 않도록 수정

### 제거

- 사용되지 않는 이전 목록 응답 DTO·변환 함수와 미사용 import 정리
- 코드 변경마다 IntentTrace 기록을 제안하던 세션 시작 훅과 해당 훅 전용 검사

### 보안

- 이스케이프된 따옴표 뒤의 비밀값이 제거되지 않고 남던 문제 수정
- IntelliJ에서 서버 주소를 바꿔도 다른 서버의 환경 변수 세션이 전송되지 않도록 사용 범위 제한
- OAuth 응답 파싱 실패 시 원인 예외에 응답 원문이 남지 않도록 처리
- 코드 심벌 이름(`symbolName`)의 비밀값·개인 home 절대 경로를 저장 전에 제거
- GitHub 사용자·저장소 권한·Check Run과 IntelliJ 응답 파싱 오류에서 원문을 포함한 원인 예외 제거

### 개발 브랜치 DB 번호 통합

메인의 V1~V6는 유지하고 개발 브랜치의 마이그레이션을 V7~V11로 통합했다.

| 개발 브랜치의 기존 번호 | 통합 번호 | 변경 |
| --- | --- | --- |
| V5 | V7 | 최초 생성 내용 해시·목록 인덱스, 변경 전 커밋 열 복원 |
| V6 | V8 | 변경 전후 코드·검증 출처 |
| V7 | V9 | 게시 시도 이력 |
| V8 | V10 | 원본 공개 기록 연결 |
| V9 | V11 | 기록 변경 이력 |

개발 브랜치의 기존 DB는 별도 이관이 필요하다. [기존 DB 업그레이드 절차](docs/operations/team-deployment.md#기존-db-업그레이드)를 따른다.

## 0.7.0 - 2026-08-30

### 추가

- IntelliJ 2025.3+에서 현재 줄에 연결된 공개 변경 의도를 조회하는 플러그인
- `its_` session을 IntelliJ PasswordSafe에 저장하고 환경 변수로 서버를 선택하는 연결 절차
- IntelliJ에서 GitHub 승인 페이지를 열고 PasswordSafe 세션을 삭제하는 액션

### 변경

- `main` 정식 릴리스 후보에서 서버/JAR, IntelliJ 플러그인, 플러그인 메타 버전을 `0.7.0`으로 통일

## 0.6.0 - 2026-08-29

첫 공개 MVP 릴리스입니다.

### 추가

- 변경 의도 초안 생성과 `DRAFT → AUTHOR_CONFIRMED → PUBLISHED → SUPERSEDED` 수명주기
- 전체 Git commit, 저장소 snapshot, 파일·줄·콘텐츠 hash와 실제 검증 결과 연결
- REST API와 Spring AI Streamable HTTP MCP 도구
- GitHub 사용자 인증, 저장소 권한 확인과 메모리 전용 `its_` session
- PR HEAD 검증 후 GitHub Check Run 게시와 재시도 시 기존 Check Run 갱신
- PostgreSQL·Caddy 기반 단일 인스턴스 팀 배포와 backup·restore 절차
- Codex skill과 개인정보를 수집하지 않는 SessionStart 안내 hook

### 변경

- 저장소 식별자와 코드 근거 경로를 입력 시점에 정규화
- 같은 요청 ID의 저장 내용까지 비교하도록 생성 멱등성 강화
- JDBC 하위 행 저장·조회를 일괄 처리
- 사용하지 않는 `baseRevision` API·도메인·DB 열 제거
- 외부 container image를 digest, GitHub Actions를 commit SHA로 고정

### 보안

- GitHub access·refresh token과 `its_` session을 프로세스 메모리에만 저장
- OAuth callback에 HttpOnly·SameSite cookie, 일회성 `state`, TTL과 PKCE `S256` 적용
- OAuth 승인 대기 요청에 전역 개수 상한 적용
- token, private key, client secret과 개인 절대 경로 redaction 강화
- 공개·대체 기록과 GitHub 게시에 저장소 권한 및 commit 일치 확인 적용
