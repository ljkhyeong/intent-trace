# IntentTrace 작업 규칙

IntentTrace는 코드 변경의 요청·판단 출처·코드 근거·검증 결과를 기록하고 작성자가 확인한 내용을 팀에 공유한다.

## 작업 방식

- 사용자 요청과 앞선 대화에서 승인한 범위까지 구현·검증을 마친다. 일반적인 구현 선택은 판단해서 진행하고 이미 받은 승인을 다시 묻지 않는다.
- 결과나 작업 범위를 결정할 정보가 부족할 때만 질문한다. 답변이 필요 없는 작업은 계속한다. 별도 승인이 필요한 작업은 검토할 결과를 준비한 뒤 묻는다.
- 스킬의 작업 절차보다 명시적인 사용자 요청을 우선한다. 스킬 때문에 작업을 멈춰야 하면 해당 파일과 적용한 문구를 밝힌다.
- 응답·문서·주석·PR 리뷰는 짧고 구체적인 한국어로 쓴다. API 이름·경로·설정 키는 원문을 유지한다. 완료 후 작업 종류를 붙인 한국어 커밋을 만든다. 예: `docs: 기록 조회 안내 정리`.
- 변경에 맞는 검증만 실행한다. 문구 수정에 테스트를 추가하거나 같은 정책을 여러 계층에서 반복 검증하지 않는다. 통과한 검증은 새 변경이나 실패 원인이 없으면 반복하지 않는다.

## 제품 규칙

- 원문 대화·숨은 추론·검증 원문 출력은 저장하지 않는다. 비밀값과 개인 절대 경로는 저장 전에 제거한다.
- 사용자 요청·커밋 메시지·작성자가 확인한 AI 요약·추론·미확인을 구분한다. 실제 실행한 검증만 기록하고 다른 스냅샷의 결과를 현재 검증으로 표시하지 않는다.
- 작성자 확인과 공개에는 전체 커밋 ID와 일치하는 스냅샷 해시가 필요하다. 공개 본문은 직접 수정하지 않고 후속 공개 기록으로 대체한다.
- 작성자는 GitHub `/user`의 숫자 ID로 정한다. 비공개 기록은 작성자만, 공개·대체 기록은 저장소 읽기 권한이 있는 팀원만 조회한다.
- 기본 REST·MCP 클라이언트에는 `its_` 세션만 전달한다. GitHub 토큰은 서버 메모리에만 두고 비밀값을 DB·로그·오류 응답·도구 인자에 넣지 않는다.
- GitHub 게시 전 서버 응답으로 대상 저장소와 PR HEAD를 확인한다. 실제 게시·배포·DB 교체는 사용자가 요청한 대상과 범위에서 수행한다.

## 필요한 문서

- 저장소를 변경할 때는 [개발 스킬](skills/intent-trace-flows/SKILL.md)에서 해당 기능의 문서와 검증을 고른다. 이미 읽은 지시는 다시 읽지 않는다.
- IntentTrace 기록을 생성·조회·관리할 때는 [사용 스킬](skills/intent-trace/SKILL.md)을 적용한다.
- 실행·설치는 [README](README.md), 이전 작업의 진행 상태·제한은 [HANDOFF](HANDOFF.md)의 관련 부분만 읽는다.
